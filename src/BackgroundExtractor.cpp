#include "../headers/BackgroundExtractor.h"
#include <iostream>

void BackgroundExtractor::extract(const std::vector<Pixel>& pixels) {
    /* TODO: пока заглушка */
    std::cout << pixels.size() << " пикселей." << std::endl;
}
